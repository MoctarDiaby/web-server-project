apiVersion: v1
kind: Service
metadata:
  name: mariadb
spec:
  selector:
    app: mariadb
  ports:
    - protocol: TCP
      port: {{ .Values.mariadb.servicePort }}
      targetPort: {{ .Values.mariadb.containerPort }}
  type: ClusterIP
