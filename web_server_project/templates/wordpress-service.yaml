apiVersion: v1
kind: Service
metadata:
  name: wordpress
spec:
  selector:
    app: wordpress
  ports:
    - protocol: TCP
      port: {{ .Values.wordpress.servicePort }}
      targetPort: {{ .Values.wordpress.containerPort }}
  type: ClusterIP
