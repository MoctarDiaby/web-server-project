apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  labels:
    app: nginx
spec:
  type: nodeport
  ports:
    - port: 8090
      targetPort: 8090
      protocol: TCP
  selector:
    app: nginx
