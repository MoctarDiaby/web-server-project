apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  labels:
    app: nginx
spec:
  type: NodePort
  selector:
     app: nginx-deployment
  ports:
    - name: http
      protocol: TCP
      port: 8090
      nodePort: 30090
